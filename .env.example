# FitGlue Local Development Environment Variables

# Project ID
# Use fitglue-server-dev for development
GOOGLE_CLOUD_PROJECT=fitglue-server-dev

# Artifacts Bucket (Created by Terraform: [project-id]-artifacts)
GCS_ARTIFACT_BUCKET=fitglue-server-dev-artifacts

# Secrets (Required to bypass Secret Manager)
HEVY_SIGNING_SECRET=local-hevy-secret
# fitbit-client-id=your-client-id
# fitbit-client-secret=your-client-secret
# keiser-USER_ID=user-token

# Configuration
# PORTS are defined in scripts/local_run.sh but can be overridden if the scripts support it.
