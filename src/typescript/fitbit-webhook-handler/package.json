{
  "name": "@fitglue/fitbit-webhook-handler",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rm -rf dist",
    "gcp-build": "npm run build",
    "test": "jest",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@fitglue/shared": "file:../../shared",
    "@google-cloud/functions-framework": "^3.3.0",
    "@google-cloud/secret-manager": "^5.0.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  }
}
