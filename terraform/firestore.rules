rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // FitGlue Strategy: All data access is mediated by Cloud Functions
    // Direct client access (via JS SDK) is denied for all collections.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
